import { PlaceholderPage } from "@/components/admin";

const Permissions = () => (
  <PlaceholderPage 
    title="Permissions" 
    description="Configure team permissions and access levels."
  />
);

export default Permissions;
